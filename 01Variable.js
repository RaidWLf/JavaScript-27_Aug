let name = "Ambuj";
let age = 21;
let isEnrolledInFSJS = true;
let experience = null;
let isJobReady;

console.log(`
${name} is ${typeof name}
${age} is ${typeof age}
${isEnrolledInFSJS} is ${typeof isEnrolledInFSJS}
${experience} is ${typeof experience}
${isJobReady} is ${typeof isJobReady}

`);
